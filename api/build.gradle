apply plugin: 'com.android.library'

android {
    compileSdkVersion 24
    buildToolsVersion '24.0.1'

    defaultConfig {
        minSdkVersion 7
        targetSdkVersion 24
        versionCode 3
        versionName "1.5.0"
    }
    buildTypes {
        release {
            minifyEnabled false
        }
    }
}