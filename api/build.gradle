apply plugin: 'com.android.library'

android {
    compileSdkVersion 25
    buildToolsVersion '24.0.3'

    defaultConfig {
        minSdkVersion 7
        targetSdkVersion 25
        versionCode 6
        versionName "1.8.0"
    }
    buildTypes {
        release {
            minifyEnabled false
        }
    }
}