ext.shouldBePublished = false
ext.notPublishingReason = 'owner unknown (may have not been published yet).'

apply from: "$rootDir/addons/gradle/pack_apk.gradle"

dependencies {
    implementation project(path: ':addons:languages:rusyn:pack')
}
