//owner unknown (may have not been published yet)
ext.shouldBePublished = false

apply from: "$rootDir/addons/gradle/pack_apk.gradle"

dependencies {
    implementation project(path: ':addons:languages:serbian:pack')
}
