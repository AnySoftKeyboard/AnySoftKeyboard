ext.shouldBePublished = true
ext.override_app_id = 'org.herrlado.ask.languagepack.lithuanian'
ext.override_release_key_alias = 'czech' //ownership taken from the same owner for czech

apply from: "$rootDir/addons/gradle/pack_apk.gradle"

dependencies {
    implementation project(path: ':addons:languages:lithuanian:pack')
}
