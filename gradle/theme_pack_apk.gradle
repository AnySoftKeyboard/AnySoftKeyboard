apply plugin: 'com.android.application'

apply from: "$rootDir/gradle/pack_apk.gradle"

def themeName = parent.name

android {
    defaultConfig {
        applicationId "com.anysoftkeyboard.theme.${themeName}"
    }
}

